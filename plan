Create an app that takes food types and quantities and returns number of calories.

What we need:
1. index file:  - connect to server (uvicorn)
                - use the nutrition API
                - methods: put(user input with foods), get(return the calories)
                -
2. sqlite database: save info from user
3. config file
4. requirements file
5. exceptions
6. tests